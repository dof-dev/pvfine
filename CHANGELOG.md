# Changelog

本文件记录 pvfine 的版本更新。

## [v0.6.0] - 2026-09-21

### ✨ 新特性

- 高级字符串搜索迁移至 SQLite 磁盘反向索引，支持大型归档的游标分页查询、查询结果缓存与按需重建。
- 新增字符串引用扫描与索引构建进度、取消搜索和结果失效处理，编辑归档后可自动重新建立索引。
- 抽象归档格式探测、内容规则与写入能力，统一支持标准格式、90CN 变体与 110US（Paged110）分页归档。

### 🐛 修复

- 修复 110US 字符串池写入与 `indexhash` 处理导致的编码和列表注册问题。
- 修复 Windows 下 SQLite 只读文件 URI 将本地盘符误解析为 authority，导致高级搜索无法打开索引的问题。

### ⚡ 优化

- 优化高级搜索索引缓存、查询结果磁盘预算和会话清理，降低大归档搜索的内存占用。
- 优化开发构建参数，保持 SQLite 依赖的编译优化，改善索引构建性能。

### 🔨 重构与维护

- 新增 SQLite 文件索引与语义索引测试，补充字符串引用、归档格式组合及编码边界回归测试。
- 更新 PVF 变体架构、索引性能与新格式分析文档。

## [v0.5.0] - 2026-09-20

### ✨ 新特性

- 新增 110US（Paged110）容器解析与写回支持，兼容分页归档及其相关密钥文件。
- 新增非标准种子 PVF 变体识别 (90CN)、种子恢复与保存支持，变体归档可正常编辑并重建 HASH 段。
- 新增 `list/*_indexhash.etc` 的解析、读写工具，以及 110US `hashindex` 的自动生成与注册界面。
- 新增 110US 字符串表、列表布局与占位符编辑支持；新建文件时可创建字符串表条目并插入引用。
- 编辑器支持显示并就地修改占位符译文。

### 🎨 界面与交互

- 仅对分页归档显示文件预览入口，避免在不支持的归档类型中展示无效操作。

### 🐛 修复

- 修复写入中文脚本时字符串池选择不当导致的游戏内乱码。
- 修复变体种子搜索中的 goroutine 泄漏。
- 修复变体归档保存时固定种子与 HASH 段处理不正确的问题，确保新增文件可被索引。

### ⚡ 优化

- 新增索引缓存，减少重复建立与查询索引的开销。

### 🔨 重构与维护

- 补充 Paged110、90CN 变体、种子恢复、HASH/indexhash、字符串表及字符串编码的回归测试。
- 更新 PVF 格式说明与 110US 容器分析文档。

## [v0.4.0] - 2026-09-13

### ✨ 新特性

- 新增 ANI 动画预览：解析 `.ani` 帧序列与贴图引用，在编辑器内直接播放角色、特效等动画资源。
- 新增装备预览：解析装备脚本字段并渲染属性面板，可查看名称、等级、职业限定、物理/魔法攻击等关键数值。
- 新增预览宿主机制，按文件类型自动分派预览组件，为后续资源预览扩展提供统一接入点。
- 脚本控制台新增 `.lst` 列表操作 API，支持按文件顺序遍历（`forEach`）、查询与改写列表条目。
- 脚本新增文件集（FileSet）读写 API，可在脚本中直接读取与创建文件集。
- 脚本新增创建、复制、删除文件 API，支持批量增删归档条目。
- 新增路径中的目录自动创建：通过脚本或新建文件时，父目录不存在会自动补建。
- 脚本工作区支持以独立窗口打开，便于与主编辑窗口并排使用。

### 🎨 界面与交互

- 编辑器改为手动保存：编辑内容经工具栏保存按钮或 <kbd>Cmd</kbd>/<kbd>Ctrl</kbd>+<kbd>S</kbd> 写入 Overlay，关闭未保存标签前提示确认。
- 文件编辑工具栏显示当前文件路径，支持点击复制。
- 资源管理器右键菜单新增复制二级菜单，可复制相对路径或绝对路径。
- 鼠标中键点击标签页可直接关闭该标签。
- 脚本控制台支持文本选中，脚本预览 Diff 新增按文件路径筛选。
- 代码编辑器 <kbd>Tab</kbd> 键改为插入制表符，不再触发整行自动缩进；优化悬浮提示展示。
- 修复文件树滚动异常，并完善磁盘保存确认弹窗的加载中状态反馈。

### 🐛 修复

- 修复较大文件 Diff 退化为整文件删除的比对错误。
- 修复标注名字索引跨越二级 section 的问题，名字索引改为只取一级 section。
- 修复 Windows 上平台相关的测试断言。

### 🔨 重构与维护

- 补充 ANI 预览、装备预览、脚本文件集、窗口服务与批量 Diff 的单元测试。
- 更新标注配置与渲染规则配置，补充脚本工作区、渲染规则与标注设计文档。
- 将应用版本元数据统一同步至 `0.4.0`，覆盖 macOS、Windows 与 Linux 构建配置。
- 真实归档集成测试改为断言归档结构而非固定文件数，避免因本地游戏版本差异误报失败。

## [v0.3.0] - 2026-09-10

### ✨ 新特性

- 新增 JavaScript 脚本工作区，使用 Goja 沙箱在进程内运行 `.pvf.js`，提供 `pvf.files/find/glob`、文本接口与结构化 Token 文档 API。
- 脚本运行阶段写入隔离事务，成功后生成可审阅预览，确认后点击「应用选中」才写入工作区 Overlay，归档仍需手动保存。
- 新增脚本渲染规则，可按扩展名与 glob 匹配定制编辑器及导出的展示格式，并支持在设置中热重载配置。
- 编辑器新增 JavaScript 语法模式、脚本 API 类型声明与语言服务补全，`.pvf.js` 与 PVF 文本共用同一编辑面板。
- 注册 `.pvf` 文件关联，支持双击或拖拽 `.pvf` 文件直接启动并打开归档。
- 标注规则新增 `offset` 跳过 section 开头 token，以及 `tokensPerLineIndex` 按行内数值动态覆盖 `recordTokens`。

### 🎨 界面与交互

- 重构设置界面，按功能分区重排配置项。
- 优化脚本工作区、版本控制面板、批处理与高级搜索界面。
- 优化侧边栏、工具栏与状态栏布局，精简冗余操作入口。
- 优化资源管理器体验，支持一键收起所有目录。
- 优化首页界面，打开文件时自动切回「归档编辑」视图。
- 编辑器标签文件名过长时自动省略显示。

### 🐛 修复

- 版本控制改用规范化渲染文本计算内容快照，避免脚本 token 字符串池引用导致的差异误判。
- 修复标注重复目标在记录宽度越界时读取 token 的行为，并在下标非法时安全跳过。

### 🔨 重构与维护

- 新增 `internal/script` 模块与 `ScriptService`、`RenderingService` 服务层接入。
- 拆分 PVF 脚本文档树与规范化文本渲染逻辑，补充脚本、渲染规则、标注与版本控制测试。
- 将应用版本元数据统一同步至 `0.3.0`，覆盖 macOS、Windows 与 Linux 构建配置。

## [v0.2.1] - 2026-09-09

### 🔨 重构与维护

- 清理 macOS、Windows 和 Linux 构建模板中的示例占位元数据。
- 统一应用名称、发布者、产品标识、版本、描述、版权及项目主页配置。
- 更新项目文档，补充当前版本控制、NPK、LST、书签簿和批处理能力说明。

## [v0.2.0] - 2026-09-09

### ✨ 新特性

- 新增 NPK 资源读取与 DXT 图像解析支持。
- 新增资源版本控制功能，支持版本仓库、对象存储及版本面板。
- 新增书签簿管理，支持嵌套书签簿、书签编辑与导航。
- 新增 LST 列表、标注与跳转能力。
- 新增资源导入、标签页操作及深浅色主题切换。

### 🎨 界面与交互

- 优化资源管理器、编辑器信息栏、工具栏及文件操作界面。
- 支持在资源管理器中标记已修改文件，并改善目录选中与加载反馈。

### 🐛 修复

- 修复浅色模式下背景颜色不正确的问题。
- 修复资源管理器选中尚未加载目录时的状态处理问题。

### ⚡ 优化

- 优化书签、标签页及版本控制相关操作体验。
- 增加 NPK、PVF、服务层和版本控制的测试与基准测试覆盖。

## [v0.1.0] - 2026-09-08

### ✨ 新特性

- 新增标注系统及可配置标注索引、装备/道具联合关联与独立标注编辑器。
- 新增文件集持久化、文件导出、最近打开文件和精确匹配能力。
- 新增批处理功能，支持批量处理 PVF 内容。
- 新增 Vim 编辑模式。
- 新增资源管理器复制文件路径和选中当前文件的能力。

### 🎨 界面与交互

- 新增分屏编辑与编辑面板拖拽调整。
- 新增资源管理器单击/双击打开方式设置。

### 🐛 修复

- 修复删除文件集后状态未正确更新的问题。
- 修复标注换行计算导致的行号偏移。
- 修复发布版本脚本在重复使用相同版本号时误报 macOS 版本字段缺失的问题。

### ⚡ 优化

- 增加未保存修改退出确认。
- 增加保存确认与源文件自动备份。
- 完善编辑器缩进处理。
- 移除不再使用的 iOS 与 Android 构建模板及任务配置。
- 补充 PVF、标注、文件集、设置、批处理及编辑器相关测试和设计文档。
